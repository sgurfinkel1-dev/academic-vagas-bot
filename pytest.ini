[pytest]
testpaths = tests
# Marcador para teste que depende de site de terceiro. O CI roda com
# `-m "not rede"`; localmente, `pytest` sem filtro roda tudo.
markers =
    rede: depende de rede externa (site de terceiro pode estar fora ou bloquear)
